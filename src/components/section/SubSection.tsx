import styled from "@emotion/styled";

const SubSection = () => {
  return <Box></Box>;
};

export default SubSection;

const Box = styled.div`
  width: 100%;
  height: 1181px;
  position: relative;
  z-index: 1;
  margin-top: -110px;
  overflow-x: hidden;
`;
